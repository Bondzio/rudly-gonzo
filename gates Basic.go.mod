RX (θ) = e−iθ/2·X = cos (θ/2) · I − i sin (θ/2) · X
RY (θ) = e−iθ/2·Y = cos (θ/2) · I − i sin (θ/2) · Y
RZ (θ) = e−iθ/2·Z = cos (θ/2) · I − i sin (θ/2) · Z
Rˆn(θ) = e−iθ/2·ˆn·~θ
= cos (θ/2) · I − i sin (θ/2) · (nXX + nYY + nZZ)
XYX = −Y XRY (θ)X = RY (−θ)
XZX = −Z XRZ (θ)X = RZ (−θ)